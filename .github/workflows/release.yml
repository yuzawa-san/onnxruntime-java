name: release

on:
  push:
    tags:
    - 'v*'

jobs:
  release:
    permissions: write-all
    runs-on: ubuntu-latest
    name: Release
    steps:
      - uses: actions/checkout@v7
      - uses: gradle/actions/wrapper-validation@v6
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: oracle
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
      - name: Get the tag
        id: get_tag
        run: echo tag=$(echo ${{ github.ref }} | cut -d / -f 3) >> $GITHUB_OUTPUT
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GHA_PR_TOKEN }}
          tag_name: ${{ github.ref }}
          name: ${{ steps.get_tag.outputs.tag }}
          generate_release_notes: true
          draft: true
          prerelease: false
      - name: Publish to maven central
        env:
          ORG_GRADLE_PROJECT_signingKeyId: ${{ secrets.PGP_KEY_ID }}
          ORG_GRADLE_PROJECT_signingKey: ${{ secrets.PGP_KEY }}
          ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.PGP_PASSWORD }}
          ORG_GRADLE_PROJECT_ossrhUsername: ${{ secrets.OSSRH_USERNAME }}
          ORG_GRADLE_PROJECT_ossrhPassword: ${{ secrets.OSSRH_PASSWORD }}
        run: |
          ./gradlew --no-daemon -s publishOnnxruntimePublicationToSonatypeRepository ${{ endsWith(steps.get_tag.outputs.tag, '-1') && 'publishOnnxruntimeCpuPublicationToSonatypeRepository publishOnnxruntimeGpuPublicationToSonatypeRepository' || '' }} closeSonatypeStagingRepository